<%- include('includes/head.ejs') %>
</head>

<body>
    <%- include('includes/navigation.ejs') %>
    <section class="section">
        

        <ul>
        <% if(items.length > 0) { %>
        
            
            <% for(let item of items) { %>
            
            <li>
                <% if(timeNow>=item.itemResolveDate) { %>
                   Warning! 
                   <% } %>
               Resolve Deadline: <%= item.itemResolveDate %> | Description: <%= item.itemDesc %> | Date created: <%= item.itemDateCreated %>
            </li>

            <% } %>
        </ul>
        
        <% } else { %>
            <h1>All issues resolved! :)</h1>
        <% } %>
    </section>
<%- include('includes/end.ejs') %>